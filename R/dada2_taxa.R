dada_taxa <- function(train, species){
  #if (is.null(passed_args$db)) {
  #  db <- readline(sprintf("What is the full file path to the reference taxonomic database you'll be using? Don't use quotes. \n\n"))
  #  tax_db <- ref_db(db)
  #}

  #taxa <- dada2::assignTaxonomy(seqtab.nochim, tax_db, multithread = multi)
  #taxa <- dada2::addSpecies(taxa, "/data/das_lab/Software/Databases/microbiome/dada2/silva_species_assignment_v138.fa.gz")
}
