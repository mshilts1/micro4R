beta_wrapper <- function(...){
  #calcBeta(...)
  #mod<-betadisper(averagedbc, metadata$CATEGORY)
  #vctrs<-mod$vectors
  #centroids<-data.frame(grps=rownames(mod$centroids),data.frame(mod$centroids))

}
